# Netlify configuration for agenticthinking.ai

# Build settings
[build]
  publish = "_site"
  command = "npm run build"

# Redirects
[[redirects]]
  from = "/meet"
  to = "https://linkedin.com/in/stevenpoitras"
  status = 307
  force = true
